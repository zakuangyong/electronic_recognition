Metadata-Version: 2.4
Name: electronic-recognition
Version: 0.1.0
Summary: RAG and vision-model component recognition for electrical PDF and PNG drawings.
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: fastapi>=0.115
Requires-Dist: Pillow>=10.0
Requires-Dist: pypdf>=5.0
Requires-Dist: PyMuPDF>=1.24
Requires-Dist: python-multipart>=0.0.18
Requires-Dist: uvicorn>=0.34
Provides-Extra: dev
Requires-Dist: pytest>=8.0; extra == "dev"

# Electronic Recognition

面向电气 PDF/PNG 图纸的独立元器件识别服务。项目从
`electrical-drawing-rag` 中抽取并精简了以下能力：

1. PDF 文本提取、拆页和高分辨率渲染；
2. PNG 方向修正与统一页面格式；
3. 组件知识库目录召回；
4. 视觉模型候选选择；
5. 原图与知识库参考图联合识别；
6. 元件代号、名称、数量、置信度和坐标框输出；
7. 页面预览、彩色标注框和 JSON 下载。
8. 从 PDF 文本层提取“信号输入”和“控制方式”配置。

项目不包含回路分类、属性枚举、标签纠错、Skill 编排和图纸专属物料映射。

## 处理流程

```text
PDF / PNG
  -> 页面解析与渲染
  -> 图纸文本 + 组件目录候选选择
  -> 候选参考图片召回
  -> 图纸页面 + 参考图片多模态识别
  -> 数量和坐标归一化
  -> API JSON + 前端表格和原图标注
```

组件知识库位于：

```text
data/index/components.json
data/index/assets/components/
```

## 安装

```powershell
cd D:\project\electronic_recognition
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -e ".[dev]"
Copy-Item .env.example .env
```

编辑 `.env`：

```dotenv
ER_LLM_BASE_URL=https://your-endpoint.example/v1
ER_LLM_API_KEY=your-api-key
ER_LLM_MODEL=your-vision-model
```

## 启动

```powershell
electronic-recognition --port 8892
```

打开：

```text
http://127.0.0.1:8892
```

## API

```bash
curl -X POST http://127.0.0.1:8892/analyze \
  -F "drawing=@drawing.png"
```

核心输出：

```json
{
  "document": "drawing.png",
  "detected_components": [
    {
      "reference_id": "gbt47287-fuse",
      "label": "熔断器",
      "code": "FU1,FU2",
      "component_type": "保护器件",
      "page": 1,
      "occurrence_count": 2,
      "confidence": 0.96,
      "regions": [
        [100, 120, 200, 260],
        [300, 120, 400, 260]
      ],
      "evidence": "图纸中的FU1和FU2"
    }
  ]
}
```

`regions` 使用页面左上角为原点、范围为 `0-1000` 的归一化坐标。

控制与信号配置位于独立字段：

```json
{
  "control_signal_configuration": {
    "signal_inputs": [
      {"category": "运行模式", "items": ["自动", "就地/手动"]}
    ],
    "control_modes": [
      {"controller": "PLC", "action": "关阀", "raw": "PLC（关阀）"}
    ],
    "text_source": "pdf_text"
  }
}
```

该信息不计入元件数量，也不绘制元件定位框。扫描件或没有文字层的
PDF 暂时返回空结果，后续可接入 OCR 作为补充。

## 迁移知识库

从原项目重新生成组件知识库：

```powershell
python scripts\migrate_component_knowledge.py `
  --source-knowledge "C:\path\electrical-drawing-rag\data\index\knowledge.json" `
  --output "data\index\components.json"
```

## 测试

```powershell
pytest -q
```
